const hashTable = () => {
    let keys = [];
    let indices = [];

    const put = (key, index) => {
        return 'you tried to push!!!';
    }

    const get = (key) => {
        return 'you tried to get!!!';
    }

    return { put, get }
}

export default hashTable;